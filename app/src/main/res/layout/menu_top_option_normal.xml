<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="@android:color/transparent">

    <com.lix.camera.ui.top.flashmenu.FlashSettingMenu
        android:id="@+id/flash_setting_menu"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:background="@android:color/transparent" />

    <ImageButton
        android:id="@+id/btn_switch_camera"
        android:layout_width="@dimen/top_menu_button_size"
        android:layout_height="@dimen/top_menu_button_size"
        android:scaleType="fitXY"
        android:layout_gravity="center"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_btn_switch"
        android:contentDescription="@null" />

    <ImageButton
        android:id="@+id/btn_adv_setting"
        android:layout_width="@dimen/top_menu_button_size"
        android:layout_height="@dimen/top_menu_button_size"
        android:layout_marginEnd="10dp"
        android:scaleType="fitXY"
        android:layout_gravity="end|center_vertical"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_btn_menu"
        android:contentDescription="@null" />

</FrameLayout>